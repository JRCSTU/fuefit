## Dependencies for the *conda* package-manager.
## Use this command to install them::
##
##    conda install `cat minconda_requirements.txt`
##
## Use the "standard" ``pip`` program to install the rest `requirements.txt` of the project.
##
pip
setuptools
#nose ## Use `pip install nose` or else `python setup.py nosetests` fails!
sphinx
numpy
scipy
pandas
matplotlib
networkx
